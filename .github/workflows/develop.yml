name: Develop
on:
  push:
    branches:
      - develop
jobs:
  create-release:
    runs-on: ubuntu-latest
    steps:
      - name: Bump version and push tag
        id: tag_version
        uses: mathieudutour/github-tag-action@v6.2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          dry_run: true
      - name: Create Branch
        run: |
          git checkout -b release/${{ steps.tag_version.outputs.new_tag }}
          git push origin release/${{ steps.tag_version.outputs.new_tag }}
      - name: Create Pull Request
        run: |
              gh pr create --base develop --head ${{ steps.tag_version.outputs.new_tag }} \
              --title "develop <- ${{ steps.tag_version.outputs.new_tag }}" \
              --body "This is an automated pull request to merge the changes from the feature branch to the development branch." \
              --label "automated-pr"
        env: 
          GH_TOKEN: ${{ github.token }}
      